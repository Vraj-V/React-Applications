const Jsx_Syntax = () => {
    return (
        <div>
            <h1>JSX Syntax</h1>
            <p>JSX stands for JavaScript XML. It allows us to write HTML in React. JSX makes it easier to write and add HTML in React.</p>
            <h2>JSX Syntax Rules</h2>

            </div>
    )
}
export default Jsx_Syntax;